package
{
    public class RegexpObject
    {
        public function RegexpObject(regexp:RegExp, type:String)
        {
            this.regexp = regexp;
            this.type = type;
        }

        public var regexp:RegExp;
        public var type:String;
    }
}